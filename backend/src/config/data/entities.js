const User = require('../../entities/user')
const File = require('../../entities/file')
const ConversionJob = require('../../entities/conversionJob')

const entities = [
  User,
  File,
  ConversionJob
]

module.exports = entities
